import tkinter as tk
'''Football Tracker alpha version'''

window = tk.Tk()
window.title("Football Tracker")
label = tk.Label(window, text = "Menu powitalne aplikacji Football Tracker")
label.pack(side = tk.BOTTOM)
tk.mainLoop()